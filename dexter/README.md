# Dexter

A local, push-to-talk mobile agent built into a piece of hardware — press a button to talk to it.

## Setup

```sh
cd dexter_handheld_mobile_agent
uv venv --python 3.12
uv pip install -e .
```

## Usage

TBD.
